import { Text } from 'react-native'

export default function Divider() {
    return (
        <Text
            style={{
                height: 1,
                borderColor: "#D0D0D0",
                borderWidth: 2,
                marginTop: 15,
            }}
        />
    );
}